from app.tools.weather import get_weather
from app.tools.calculator import calculate
from app.tools.dictionary import define_word

TOOLS = {
    "weather": get_weather,
    "calculator": calculate,
    "dictionary": define_word,
}
